# Use mock/simulated data for development (set to false for live API)
PUBLIC_USE_MOCK_DATA=true

# Configure your Pico IP and port here (used when PUBLIC_USE_MOCK_DATA=false)
PUBLIC_PICO_IP=192.168.1.100
PUBLIC_PICO_PORT=8080
